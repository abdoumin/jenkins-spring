
services:
#    environment:
#      RDS_HOSTNAME: mysqldb
#      RDS_PORT: 3306
#      RDS_DB_NAME: estivage
#      RDS_USERNAME: root
#      RDS_PASSWORD: user
#    stdin_open: true
#    tty: true
    
  mysqldb:
    image: abdoumin/mysql:latest
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=user
      - MYSQL_DATABASE=estivage
    ports:
      - 3308:3306
    volumes:
      - db:/var/lib/mysql
    networks:
      - frontend
  app:
    depends_on:
      - mysqldb
    image : 
      abdoumin/fullstackbackendapp:latest
    restart: on-failure
    ports:
      - 6868:8080
#    environment:
#      SPRING_APPLICATION_JSON: '{
#        "spring.datasource.url"  : "jdbc:mysql://mysqldb/estivage",
#        "spring.datasource.username" : "root",
#        "spring.datasource.password" : "user",
#        "spring.jpa.properties.hibernate.dialect" : "org.hibernate.dialect.MySQL5InnoDBDialect",
#        "spring.jpa.hibernate.ddl-auto" : "update"
#      }'
    volumes:
      - .m2:/root/.m2
#    stdin_open: true
#    tty: true
    networks:
      - backend
      - frontend

#  webserver:
#    depends_on:
#      - front-end
#    image: nginx:latest
#    ports:
#      - 4200:80
#    restart: always
#    volumes:
#      - ./nginx/conf/:/etc/nginx/conf.d/:ro
#    networks:
#      - submission-app

  front-end:
    image : abdoumin/fullstackwebapp:latest
    restart: always
    networks:
      - frontend
    tty: true
    ports:
      - 3000:80
    depends_on:
      - app


volumes:
  db:

networks:
  backend:
  frontend:
